language: minimal

branches:
  only:
    - master
    - /^\d\.\d$/

jobs:
  include:
    - stage: Unit tests
    - env: PHP=7.4
    - stage: Code quality
      script: make cs psalm

script: make install phpunit

cache:
  directories:
    - var/
    - bin/.phpunit/
